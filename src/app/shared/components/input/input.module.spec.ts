import { InputModule } from './input.module';

describe('InputModule', () => {
  const module: InputModule = new InputModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
