import { InputCheckboxComponent } from './input-checkbox.component';

describe('InputCheckboxComponent', () => {
  let component: InputCheckboxComponent;

  beforeEach(() => {
    component = new InputCheckboxComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
